
# stub makefile -- sole purpose is to assist building tarball
# distributions.  for win32 we aren't as rigorous about producing
# this list of source as we are for the other RoadMap
# directories.  (using wildcards will do for now.)

sourcelist:
	@ls *.[ch] *.cpp *.ico \
	    roadmap.rc \
	    wince_env/Roadmap.vcw \
	    wince_env/*/*.vcp \
	    icons/rm_*.bmp
